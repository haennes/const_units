symbol = "m"

[names]
# [language code (ISO 639) (not enforced but to provide good compatability), [singular, plural]]
EN = { singular = "meter", plural = "meters"]
DE = { singular = "Meter } # no need to specify if the same

derive_prefixes = [metric]
# any of the names of the files in the prefixes folder (emitting the .toml ending)


